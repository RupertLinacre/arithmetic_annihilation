{
  "name": "arithmetic-annihilation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "optimize:sprites": "node scripts/optimize-sprites.mjs",
    "prebuild": "npm run optimize:sprites",
    "build": "tsc --noEmit && vite build",
    "predeploy": "npm run build",
    "deploy": "node -e \"require('gh-pages').clean()\" && gh-pages -d dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "maths-game-problem-generator": "1.1.0",
    "phaser": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "gh-pages": "^6.3.0",
    "sharp": "^0.34.5",
    "typescript": "latest",
    "vitest": "latest"
  }
}
